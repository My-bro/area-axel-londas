apiVersion: apps/v1
kind: Deployment
metadata:
  name: prayer-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prayer-service
  template:
    metadata:
      labels:
        app: prayer-service
    spec:
      imagePullSecrets:
        - name: arearegistrykey
      containers:
      - name: prayer-container
        image: julesreyn/area:prayer
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
