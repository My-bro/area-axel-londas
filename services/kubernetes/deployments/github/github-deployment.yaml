apiVersion: apps/v1
kind: Deployment
metadata:
  name: github-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: github-service
  template:
    metadata:
      labels:
        app: github-service
    spec:
      imagePullSecrets:
        - name: arearegistrykey
      containers:
      - name: github-container
        image: julesreyn/area:github
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
